<core:FragmentDefinition
   xmlns="sap.m"
   xmlns:core="sap.ui.core">
   <Dialog
      id="helloDialog"
      title="{LocalModel>/recipient/name}"
      >
      <beginButton>
         <Button
            text="{i18n>Close}"
            press=".onCloseDialog"/>
      </beginButton>
      </Dialog>
</core:FragmentDefinition>